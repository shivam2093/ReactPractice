import React from 'react'
//we have seen so far 
// useReducer - for local state management(within Component)
// Share state between components - Global state Management
//we can acheive with useReducer + useContext

function ReducerWithuseContext() {
  return (
    <div>ReducerWithuseContext</div>
  )
}

export default ReducerWithuseContext